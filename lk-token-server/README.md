# LiveDots — LiveKit Token Server (Vercel)

This server gives out access tokens for LiveKit rooms.

## Setup
- Deploy to Vercel
- Add environment variables:
  - LIVEKIT_WS_URL
  - LIVEKIT_API_KEY
  - LIVEKIT_API_SECRET
